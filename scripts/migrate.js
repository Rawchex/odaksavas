// Run with: node --env-file=.env scripts/migrate.js const fs = require('fs'); const path = require('path'); const db = require('../server/db');  console.log('[Migration] Starting database migration...');  const schemaPath = path.join(__dirname, '..', 'schema.sql'); if (fs.existsSync(schemaPath)) {   const schema = fs.readFileSync(schemaPath, 'utf8');   db.exec(schema, err => {     if (err) console.error('Schema apply error:', err.message);     else console.log('Schema applied successfully');   }); }  // Migration checks for existing databases db.serialize(() => {   db.run(`CREATE TABLE IF NOT EXISTS party_channels (     id INTEGER PRIMARY KEY AUTOINCREMENT,     party_id INTEGER NOT NULL,     name TEXT NOT NULL,     user_limit INTEGER DEFAULT 0,     position INTEGER DEFAULT 0,     is_default INTEGER DEFAULT 0,     allow_screen_share INTEGER DEFAULT 0,     created_at DATETIME DEFAULT CURRENT_TIMESTAMP,     FOREIGN KEY (party_id) REFERENCES parties(id)   )`);    db.run(`ALTER TABLE party_members ADD COLUMN channel_id INTEGER DEFAULT NULL`, () => {});   db.run(`ALTER TABLE party_members ADD COLUMN role VARCHAR DEFAULT 'member'`, () => {});   db.run(`ALTER TABLE party_channels ADD COLUMN allow_screen_share INTEGER DEFAULT 0`, () => {});   db.run(`ALTER TABLE users ADD COLUMN google_id TEXT`, () => {});   db.run(`ALTER TABLE users ADD COLUMN email TEXT`, () => {});   db.run(`ALTER TABLE users ADD COLUMN blunk_coins INTEGER DEFAULT 0`, () => {});   db.run(`ALTER TABLE users ADD COLUMN has_premium_pass INTEGER DEFAULT 0`, () => {});   db.run(`ALTER TABLE users ADD COLUMN birth_date TEXT`, () => {});   db.run(`ALTER TABLE users ADD COLUMN equipped_banner TEXT`, () => {});   db.run(`ALTER TABLE users ADD COLUMN equipped_theme TEXT`, () => {});   db.run(`ALTER TABLE users ADD COLUMN equipped_emoji TEXT`, () => {}); });  // Database setup db.serialize(() => {   db.run(`CREATE TABLE IF NOT EXISTS user_inventory (     id INTEGER PRIMARY KEY AUTOINCREMENT,     user_id INTEGER,     item_id TEXT,     item_type TEXT,     claimed_at DATETIME DEFAULT CURRENT_TIMESTAMP   )`);    db.run(`CREATE TABLE IF NOT EXISTS users (     id INTEGER PRIMARY KEY AUTOINCREMENT,     username TEXT UNIQUE NOT NULL,     bio TEXT,     height INTEGER,     weight INTEGER,     cv TEXT,     profile_photo TEXT,     total_focus_time INTEGER DEFAULT 0,     level INTEGER DEFAULT 1,     xp INTEGER DEFAULT 0,     created_at DATETIME DEFAULT CURRENT_TIMESTAMP   )`);    db.run(`CREATE TABLE IF NOT EXISTS sessions (     id INTEGER PRIMARY KEY AUTOINCREMENT,     user_id INTEGER,     start_time DATETIME,     end_time DATETIME,     duration INTEGER,     status TEXT,     party_id INTEGER,     mode TEXT DEFAULT 'free',     target_duration INTEGER DEFAULT 0,     break_duration INTEGER DEFAULT 0,     feeling TEXT,     category TEXT,     activity TEXT,     FOREIGN KEY (user_id) REFERENCES users(id),     FOREIGN KEY (party_id) REFERENCES parties(id)   )`);   db.run('ALTER TABLE sessions ADD COLUMN mode TEXT', () => {});   db.run('ALTER TABLE sessions ADD COLUMN target_duration INTEGER', () => {});   db.run('ALTER TABLE sessions ADD COLUMN break_duration INTEGER', () => {});   db.run('ALTER TABLE sessions ADD COLUMN note TEXT', () => {});   db.run('ALTER TABLE sessions ADD COLUMN pomo_state TEXT DEFAULT "focusing"', () => {});   db.run('ALTER TABLE sessions ADD COLUMN pomo_round INTEGER DEFAULT 0', () => {});   db.run('ALTER TABLE sessions ADD COLUMN state_start_time DATETIME', () => {});    db.run(`CREATE TABLE IF NOT EXISTS parties (     id INTEGER PRIMARY KEY AUTOINCREMENT,     owner_id INTEGER,     name TEXT,     is_private INTEGER DEFAULT 0,     created_at DATETIME DEFAULT CURRENT_TIMESTAMP,     FOREIGN KEY (owner_id) REFERENCES users(id)   )`);    db.run(`CREATE TABLE IF NOT EXISTS party_invites (     id INTEGER PRIMARY KEY AUTOINCREMENT,     party_id INTEGER,     from_user_id INTEGER,     to_user_id INTEGER,     status TEXT DEFAULT 'pending',     created_at DATETIME DEFAULT CURRENT_TIMESTAMP,     FOREIGN KEY (party_id) REFERENCES parties(id),     FOREIGN KEY (from_user_id) REFERENCES users(id),     FOREIGN KEY (to_user_id) REFERENCES users(id),     UNIQUE(party_id, to_user_id)   )`);    db.run(`CREATE TABLE IF NOT EXISTS party_members (     id INTEGER PRIMARY KEY AUTOINCREMENT,     party_id INTEGER,     user_id INTEGER,     joined_at DATETIME DEFAULT CURRENT_TIMESTAMP,     FOREIGN KEY (party_id) REFERENCES parties(id),     FOREIGN KEY (user_id) REFERENCES users(id),     UNIQUE(party_id, user_id)   )`);    db.run(`CREATE TABLE IF NOT EXISTS friendships (     id INTEGER PRIMARY KEY AUTOINCREMENT,     user_id INTEGER,     friend_id INTEGER,     status TEXT DEFAULT 'pending',     created_at DATETIME DEFAULT CURRENT_TIMESTAMP,     FOREIGN KEY (user_id) REFERENCES users(id),     FOREIGN KEY (friend_id) REFERENCES users(id),     UNIQUE(user_id, friend_id)   )`);    db.run(`CREATE TABLE IF NOT EXISTS posts (     id INTEGER PRIMARY KEY AUTOINCREMENT,     user_id INTEGER,     content TEXT,     image TEXT,     created_at DATETIME DEFAULT CURRENT_TIMESTAMP,     FOREIGN KEY (user_id) REFERENCES users(id)   )`);    db.run(`CREATE TABLE IF NOT EXISTS likes (     id INTEGER PRIMARY KEY AUTOINCREMENT,     user_id INTEGER,     post_id INTEGER,     created_at DATETIME DEFAULT CURRENT_TIMESTAMP,     FOREIGN KEY (user_id) REFERENCES users(id),     FOREIGN KEY (post_id) REFERENCES posts(id),     UNIQUE(user_id, post_id)   )`);    db.run(`CREATE TABLE IF NOT EXISTS comments (     id INTEGER PRIMARY KEY AUTOINCREMENT,     user_id INTEGER,     post_id INTEGER,     content TEXT,     created_at DATETIME DEFAULT CURRENT_TIMESTAMP,     FOREIGN KEY (user_id) REFERENCES users(id),     FOREIGN KEY (post_id) REFERENCES posts(id)   )`);    db.run(`CREATE TABLE IF NOT EXISTS comment_likes (     id INTEGER PRIMARY KEY AUTOINCREMENT,     user_id INTEGER,     comment_id INTEGER,     created_at DATETIME DEFAULT CURRENT_TIMESTAMP,     FOREIGN KEY (user_id) REFERENCES users(id),     FOREIGN KEY (comment_id) REFERENCES comments(id),     UNIQUE(user_id, comment_id)   )`);    db.run(`CREATE TABLE IF NOT EXISTS notifications (     id INTEGER PRIMARY KEY AUTOINCREMENT,     user_id INTEGER,     type TEXT,     from_user_id INTEGER,     post_id INTEGER,     comment_id INTEGER,     party_id INTEGER,     read INTEGER DEFAULT 0,     created_at DATETIME DEFAULT CURRENT_TIMESTAMP,     FOREIGN KEY (user_id) REFERENCES users(id),     FOREIGN KEY (from_user_id) REFERENCES users(id)   )`);    db.run(`CREATE TABLE IF NOT EXISTS reposts (     id INTEGER PRIMARY KEY AUTOINCREMENT,     user_id INTEGER,     post_id INTEGER,     created_at DATETIME DEFAULT CURRENT_TIMESTAMP,     FOREIGN KEY (user_id) REFERENCES users(id),     FOREIGN KEY (post_id) REFERENCES posts(id),     UNIQUE(user_id, post_id)   )`);    db.run(`CREATE TABLE IF NOT EXISTS messages (     id INTEGER PRIMARY KEY AUTOINCREMENT,     from_user_id INTEGER NOT NULL,     to_user_id INTEGER NOT NULL,     content TEXT NOT NULL,     read INTEGER DEFAULT 0,     created_at DATETIME DEFAULT CURRENT_TIMESTAMP,     FOREIGN KEY (from_user_id) REFERENCES users(id),     FOREIGN KEY (to_user_id) REFERENCES users(id)   )`);    // Retroactive session activity/category whitespace & normalization cleanup   db.run("UPDATE sessions SET category = TRIM(category), activity = TRIM(activity) WHERE category IS NOT NULL OR activity IS NOT NULL", () => {});    // Add new columns to existing tables safely   db.run('ALTER TABLE users ADD COLUMN password_hash TEXT', () => {});   db.run('ALTER TABLE users ADD COLUMN last_seen DATETIME', () => {});   db.run('ALTER TABLE users ADD COLUMN is_private INTEGER DEFAULT 0', () => {});   db.run('ALTER TABLE messages ADD COLUMN parent_id INTEGER', () => {});   db.run('ALTER TABLE messages ADD COLUMN group_id INTEGER', () => {}); // for group chats   db.run('ALTER TABLE users ADD COLUMN status VARCHAR DEFAULT "online"', () => {});   db.run('ALTER TABLE messages ADD COLUMN is_share INTEGER DEFAULT 0', () => {});   db.run('ALTER TABLE parties ADD COLUMN invite_code TEXT', () => {     // Fill empty invite codes for existing parties     db.all('SELECT id FROM parties WHERE invite_code IS NULL OR invite_code = ""', (err, rows) => {       if (rows && rows.length > 0) {         rows.forEach(r => {           const code = Array.from({length: 8}, () => 'ABCDEFGHJKLMNPQRSTUVWXYZabcdefghijkmnpqrstuvwxyz23456789'[Math.floor(Math.random() * 56)]).join('');           db.run('UPDATE parties SET invite_code = ? WHERE id = ?', [code, r.id]);         });       }     });   });   db.run('ALTER TABLE sessions ADD COLUMN party_id INTEGER', () => {});   db.run('ALTER TABLE sessions ADD COLUMN feeling TEXT', () => {});   db.run('ALTER TABLE sessions ADD COLUMN category TEXT', () => {});   db.run('ALTER TABLE sessions ADD COLUMN activity TEXT', () => {});   db.run('ALTER TABLE comments ADD COLUMN parent_id INTEGER', () => {});   db.run('ALTER TABLE posts ADD COLUMN repost_of_post_id INTEGER', () => {});   db.run('ALTER TABLE posts ADD COLUMN views INTEGER DEFAULT 0', () => {});   db.run('ALTER TABLE users ADD COLUMN device_type TEXT DEFAULT \'desktop\'', () => {});   db.run('ALTER TABLE chat_groups ADD COLUMN disappearing_hours INTEGER DEFAULT 24', () => {});   db.run('ALTER TABLE chat_groups ADD COLUMN avatar TEXT', () => {});    db.run(`CREATE TABLE IF NOT EXISTS categories (     id INTEGER PRIMARY KEY AUTOINCREMENT,     name TEXT UNIQUE NOT NULL,     slug TEXT UNIQUE NOT NULL,     icon TEXT,     created_at DATETIME DEFAULT CURRENT_TIMESTAMP   )`);    db.run(`CREATE TABLE IF NOT EXISTS tags (     id INTEGER PRIMARY KEY AUTOINCREMENT,     name TEXT UNIQUE NOT NULL,     slug TEXT UNIQUE NOT NULL,     created_at DATETIME DEFAULT CURRENT_TIMESTAMP   )`);    db.run('ALTER TABLE sessions ADD COLUMN category_id INTEGER REFERENCES categories(id)', () => {});   db.run('ALTER TABLE sessions ADD COLUMN tag_id INTEGER REFERENCES tags(id)', () => {});    // Seed Default Categories   const seedCategories = [     // Original     { name: 'Ders & Akademik', slug: 'ders', icon: 'fa-graduation-cap' },     { name: '�� & Kariyer', slug: 'is', icon: 'fa-briefcase' },     { name: 'Yaz�l�m & Teknoloji', slug: 'yazilim', icon: 'fa-code' },     { name: 'Okuma & Ara�t�rma', slug: 'okuma', icon: 'fa-book-open' },     { name: 'Sanat & Tasar�m', slug: 'sanat-tasarim', icon: 'fa-palette' },     { name: 'Dil ��renimi', slug: 'dil', icon: 'fa-language' },     { name: 'Di�er', slug: 'diger', icon: 'fa-cube' },     // 1:1 Matches for activities.js Tabs     { name: 'Hobi', slug: 'hobi', icon: 'fa-gamepad' },     { name: 'KPSS Lisans', slug: 'kpss-lisans', icon: 'fa-file-signature' },     { name: 'KPSS �n Lisans', slug: 'kpss-on-lisans', icon: 'fa-file-signature' },     { name: 'KPSS Orta��retim', slug: 'kpss-ortaogretim', icon: 'fa-file-signature' },     { name: 'KPSS E�itim Bilimleri & �ABT', slug: 'kpss-egitim-oabt', icon: 'fa-file-signature' },     { name: 'YKS & Lise', slug: 'yks-lise', icon: 'fa-school' },     { name: 'Ortaokul & LGS', slug: 'ortaokul-lgs', icon: 'fa-child' },     { name: 'E�itim', slug: 'egitim', icon: 'fa-book' },     { name: '�� & Proje', slug: 'is-proje', icon: 'fa-laptop-code' },     { name: 'Sanat', slug: 'sanat', icon: 'fa-paint-brush' },     { name: 'Sa�l�k', slug: 'saglik', icon: 'fa-heartbeat' }   ];    seedCategories.forEach(cat => {     db.run('INSERT OR IGNORE INTO categories (name, slug, icon) VALUES (?, ?, ?)', [cat.name, cat.slug, cat.icon]);   });    // Performance: DB indexes for message queries   db.run('CREATE INDEX IF NOT EXISTS idx_messages_dm ON messages(from_user_id, to_user_id, group_id)');   db.run('CREATE INDEX IF NOT EXISTS idx_messages_read ON messages(from_user_id, to_user_id, read)');   db.run('CREATE INDEX IF NOT EXISTS idx_messages_group ON messages(group_id, created_at)');   db.run('CREATE INDEX IF NOT EXISTS idx_messages_created ON messages(created_at)');   db.run('CREATE INDEX IF NOT EXISTS idx_message_reactions_msg ON message_reactions(message_id)');      db.run(`CREATE TABLE IF NOT EXISTS chat_groups (     id INTEGER PRIMARY KEY AUTOINCREMENT,     name TEXT NOT NULL,     avatar TEXT,     disappearing_hours INTEGER DEFAULT 24,     created_by INTEGER,     created_at DATETIME DEFAULT CURRENT_TIMESTAMP,     FOREIGN KEY (created_by) REFERENCES users(id)   )`);   db.run(`CREATE TABLE IF NOT EXISTS chat_settings (     id INTEGER PRIMARY KEY AUTOINCREMENT,     user1_id INTEGER NOT NULL,     user2_id INTEGER NOT NULL,     disappearing_hours INTEGER DEFAULT 24,     UNIQUE(user1_id, user2_id),     FOREIGN KEY (user1_id) REFERENCES users(id),     FOREIGN KEY (user2_id) REFERENCES users(id)   )`);      db.run(`CREATE TABLE IF NOT EXISTS chat_group_members (     group_id INTEGER,     user_id INTEGER,     joined_at DATETIME DEFAULT CURRENT_TIMESTAMP,     PRIMARY KEY (group_id, user_id),     FOREIGN KEY (group_id) REFERENCES chat_groups(id),     FOREIGN KEY (user_id) REFERENCES users(id)   )`);      db.run(`CREATE TABLE IF NOT EXISTS party_messages (     id INTEGER PRIMARY KEY AUTOINCREMENT,     party_id INTEGER,     user_id INTEGER,     content TEXT,     created_at DATETIME DEFAULT CURRENT_TIMESTAMP,     FOREIGN KEY (party_id) REFERENCES parties(id),     FOREIGN KEY (user_id) REFERENCES users(id)   )`);    db.run(`CREATE TABLE IF NOT EXISTS message_reactions (     id INTEGER PRIMARY KEY AUTOINCREMENT,     message_id INTEGER,     user_id INTEGER,     reaction TEXT,     created_at DATETIME DEFAULT CURRENT_TIMESTAMP,     UNIQUE(message_id, user_id),     FOREIGN KEY (message_id) REFERENCES messages(id) ON DELETE CASCADE,     FOREIGN KEY (user_id) REFERENCES users(id)   )`);    db.run(`CREATE TABLE IF NOT EXISTS web_push_subscriptions (     id INTEGER PRIMARY KEY AUTOINCREMENT,     user_id INTEGER,     subscription TEXT NOT NULL,     created_at DATETIME DEFAULT CURRENT_TIMESTAMP,     FOREIGN KEY (user_id) REFERENCES users(id)   )`);    db.run(`CREATE TRIGGER IF NOT EXISTS ignore_dnd_notifications     BEFORE INSERT ON notifications     FOR EACH ROW     WHEN (SELECT status FROM users WHERE id = NEW.user_id) = 'dnd'     BEGIN       SELECT RAISE(IGNORE);     END;   `);    db.run(`CREATE TABLE IF NOT EXISTS party_bans (     id INTEGER PRIMARY KEY AUTOINCREMENT,     party_id INTEGER NOT NULL,     user_id INTEGER NOT NULL,     banned_by INTEGER NOT NULL,     reason TEXT,     created_at DATETIME DEFAULT (datetime('now')),     UNIQUE(party_id, user_id)   )`);    db.run(`CREATE TABLE IF NOT EXISTS party_voice_moderation (     id INTEGER PRIMARY KEY AUTOINCREMENT,     party_id INTEGER NOT NULL,     user_id INTEGER NOT NULL,     muted_by INTEGER NOT NULL,     reason TEXT,     expires_at DATETIME,     created_at DATETIME DEFAULT (datetime('now')),     UNIQUE(party_id, user_id)   )`);    db.run(`CREATE TABLE IF NOT EXISTS party_moderation_audit (     id INTEGER PRIMARY KEY AUTOINCREMENT,     party_id INTEGER NOT NULL,     actor_id INTEGER NOT NULL,     target_user_id INTEGER,     action TEXT NOT NULL,     reason TEXT,     metadata TEXT,     created_at DATETIME DEFAULT (datetime('now'))   )`);   db.run(`CREATE TABLE IF NOT EXISTS orders (     id INTEGER PRIMARY KEY AUTOINCREMENT,     user_id INTEGER NOT NULL,     order_id TEXT NOT NULL UNIQUE,     package_id TEXT,     price REAL,     coins INTEGER,     status TEXT DEFAULT 'pending',     shopier_order_no TEXT,     created_at DATETIME DEFAULT (datetime('now')),     updated_at DATETIME DEFAULT (datetime('now'))   )`); });  setTimeout(() => {   console.log('[Migration] Migration commands dispatched.');   process.exit(0); }, 2000); 